---
#roles to be installed
#to install these roles, use command:
#    sudo ansible-galaxy install -r roles.yml

 - leonidas.nvm
 - geerlingguy.nginx
 - geerlingguy.mysql
 - geerlingguy.git
 - dirn.tmux